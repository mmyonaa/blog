# 설계/작업용 문서 (로컬 전용, 추적 안 함)
# 주의: `docs/`로 디렉토리째 무시하면 아래 예외(!)가 안 먹는다 — 내용물 패턴(docs/*)이어야 한다.
docs/*
# 예외: 오케스트레이터 학습 기록(#43)은 CI 러너에서도 누적돼야 하므로 추적
!docs/orchestrator-learnings.md

# 세션 인계 기록 (로컬 전용 — 레포가 public이라 추적하지 않는다)
handoff/

# Node / TypeScript
node_modules/
dist/
*.tsbuildinfo
*.log
.env
.env.*
.pnpm-store/

# Astro
.astro/

# Playwright MCP: 브라우저 조작 시 자동 생성되는 페이지 스냅샷(로컬 임시)
.playwright-mcp/
# 브라우저 스크린샷을 디렉토리 없는 파일명으로 찍으면 레포 루트(cwd)로 떨어진다.
# .playwright-mcp/ 밖으로 새는 그런 임시 캡처를 잡는 안전망(루트 이미지는 프로젝트 자산이 아님).
/*.png
/*.jpg
/*.jpeg

# Claude Code 로컬 설정 (개인용)
.claude/settings.local.json

# OS
.DS_Store
